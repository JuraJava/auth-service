package com.yurdan.authService.model.enums;

import lombok.Getter;

@Getter
public enum RoleName {
    USER,
    ADMIN,
    RECEIVER,
    STOREKEEPER,
    ENGINEER,
    ADMINISTRATOR,
    DIRECTOR;
}
