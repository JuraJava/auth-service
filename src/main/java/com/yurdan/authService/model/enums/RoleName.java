package com.yurdan.authService.model.enums;

import lombok.Getter;

@Getter
public enum RoleName {
    ADMIN,
    USER,
    RECEIVER,
    STOREKEEPER,
    ENGINEER,
    ADMINISTRATOR,
    DIRECTOR;
}
